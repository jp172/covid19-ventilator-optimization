# the solver should do the simulation, i.e., go through time steps, call the scheduler if a new request comes in, etc.
# to this end the requests should be sorted in terms of time.


def solve(hospital_data, request_data, scheduler):
    # i.e. for r in requests, schedule the request

    return
